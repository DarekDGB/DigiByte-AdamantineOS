# Changelog

## Unreleased

### Foundation
- EQC v1: deterministic context hash + minimal evaluator
- WSQKAuthority v1: issued_at/expires_at/nonce
- TVA: fail-closed binding + deterministic time window + injected nonce replay protection
- Tests: negative-first + allow path
